export default function Loader({ size='sm' }) {
    if (size === 'sm'){
        return (
            <span className="loader-sm"></span>
        )
    }

    return (
        <span className="loader-sm"></span>
    )
}